import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "FhenixDate - Encrypted Dating",
  description: "Find your perfect match with fully encrypted privacy. Your preferences remain confidential, powered by Fhenix FHE.",
  keywords: ["dating", "encrypted", "privacy", "blockchain", "FHE", "Fhenix"],
  openGraph: {
    title: "FhenixDate - Encrypted Dating",
    description: "Find your perfect match with fully encrypted privacy.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
